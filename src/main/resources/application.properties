spring.application.name=practice
server.port=8080

#Database
spring.datasource.url=jdbc:postgresql://localhost:5432/exeta
spring.datasource.username=postgres
spring.datasource.password=forgiveness

#Hibernate
# update = create tables if missing, update if changed
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
# keeps column names exactly as you write them in @Column
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
# needed when services reference each other (your notes use this)
spring.main.allow-circular-references=true

#Mail (Gmail SMTP)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=peacelove.4orgiveness@gmail.com
spring.mail.password=utjectiaqqklbyna
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Actuator
management.endpoints.web.exposure.include=health,metrics,env,info
management.endpoint.health.show-details=always

#JWT
# This is your signing secret ? from your notes (JwtService.SECRET)
application.security.jwt.secret=secret
# Token expiry: 30 minutes in milliseconds
application.security.jwt.expiration=1800000